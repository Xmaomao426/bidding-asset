Prompt version: semantic-shadow/v2

You extract only facts explicitly supported by the supplied source. Return one JSON object and no prose.

Rules:
1. Do not use outside knowledge. Do not infer a business role merely from an organization name.
2. Every known document type or fact must cite one or more exact substrings from title or text. Set evidence.source to "title" or "text".
3. If the source does not reliably support a value or role, return unknown. Do not fill fields for completeness.
4. Keep amount roles separate: budget, ceiling, award, contract, or other. Keep date roles explicit.
5. Emit repeated facts for multiple suppliers, winners, amounts, lots, or packages. Use diagnostics to describe relationships that the current flat schema cannot represent safely.
6. A supplier, bidder, respondent, candidate, ranked entity, or merely mentioned organization is not a winner. Do not infer a final award from participation, ranking, or an organization name.
7. A known winner requires an exact evidence substring that contains both the entity name and explicit positive result wording such as 中标、成交、授标、or 入选. Evidence containing only the entity name is insufficient.
8. 未中标、未成交、未入围、投标无效、响应无效、被否决、废标、or 取消中标 excludes that entity from winner. If positive and negative result evidence conflict, return unknown or conflict with an empty value.
9. Do not treat 中标候选人、成交候选人、排名第一、or other candidate/ranking language as a final winner without separate explicit final award evidence.
10. If multiple entities appear, bind each winner independently to its own positive result evidence. Do not copy winner status across suppliers, lots, or packages.
11. Parser warnings and incomplete text are evidence that you may need to abstain; never reconstruct missing content.
12. Allowed statuses are known, unknown, ambiguous, conflict. Only known may contain a non-empty value.
13. Allowed field_name values are project_name, project_number, customer, winner, amount, project_content, date.

Output shape:
{
  "document_type": {
    "status": "known|unknown|ambiguous|conflict",
    "value": "",
    "alternatives": [],
    "evidence": [{"source": "title|text", "text": "exact source substring"}]
  },
  "facts": [
    {
      "field_name": "project_name|project_number|customer|winner|amount|project_content|date",
      "role": "supplier|budget|ceiling|award|contract|bid_open_time|publish_date|signing_date|other|",
      "status": "known|unknown|ambiguous|conflict",
      "value": "",
      "alternatives": [],
      "evidence": [{"source": "title|text", "text": "exact source substring"}]
    }
  ],
  "diagnostics": {
    "input_incomplete": false,
    "multi_subject_or_amount_relationships": [],
    "notes": []
  }
}

Source document JSON:
{{DOCUMENT_JSON}}
